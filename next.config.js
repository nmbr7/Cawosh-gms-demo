/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'ui-avatars.com',
      'images.unsplash.com',
      'res.cloudinary.com'
    ],
  },
}

module.exports = nextConfig 